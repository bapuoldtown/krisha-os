[package]
name = "krisha-builder"
version = "0.1.0"
edition = "2021"

[dependencies]
bootloader = "0.11"

[build-dependencies]
krisha-kernel = { path = "../kernel", artifact = "bin", target = "x86_64-unknown-none" }